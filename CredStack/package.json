{
  "name": "cred-stack",
  "version": "1.0.0",
  "description": "Verifiable credential issuance, chains, and delegation on ProbeChain",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:rydberg": "npx hardhat run scripts/deploy.ts --network rydberg"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  }
}
