{
  "name": "probe-chess2",
  "version": "1.0.0",
  "description": "Advanced chess tournament with Swiss-system pairing on ProbeChain Rydberg Testnet",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network rydberg",
    "deploy:local": "hardhat run scripts/deploy.ts --network hardhat"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  }
}
