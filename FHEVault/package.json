{
  "name": "fhe-vault",
  "version": "1.0.0",
  "description": "Encrypted computation escrow on ProbeChain Rydberg Testnet",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network rydberg",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  }
}
