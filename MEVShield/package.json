{
  "name": "mev-shield",
  "version": "1.0.0",
  "description": "MEV protection via commit-reveal encrypted transactions on ProbeChain",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network rydberg",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  }
}
