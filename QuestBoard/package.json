{
  "name": "quest-board",
  "version": "1.0.0",
  "description": "Learn-to-earn quest system with badge NFTs on ProbeChain Rydberg Testnet",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network rydberg",
    "test": "hardhat test"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  }
}
